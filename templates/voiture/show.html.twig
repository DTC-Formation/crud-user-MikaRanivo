{% extends "base.html.twig" %}
{% block title %}Information Voiture{% endblock %}

{% block body %}
<div class="container">
<table class="table">
    <h1>Voiture</h1>
    <br>
    <tbody>
        <tr>
            <th>Id :</th>
            <td> {{ voiture.id }} </td>
        </tr>
        <tr>
            <th>Type :</th>
            <td>{{voiture.type }}</td>
        </tr>
        <tr>
            <th>Modele</th>
            <td>{{voiture.modele }}</td>
        </tr>
         <tr>
            <th>Couleur</th>
            <td>{{voiture.color }}</td>
        </tr>
         <tr>
            <th>Immatriculation</th>
            <td>{{voiture.number }}</td>
        </tr>
         <tr>
            <th>Marque</th>
            <td>{{voiture.marque }}</td>
        </tr>
    </tbody>
</table>
<br>
<div class="d-flex" >
 <a class="btn btn-primary me-2" href =" {{ path('app_voiture_index') }} " > Liste Voitures</a>
 <a class="btn btn-warning me-2" href = "{{ path('app_voiture_edit', {'id' : voiture.id}) }}"> Edit</a>
 {{ include('voiture/delete.html.twig') }}
</div>
</div>
{% endblock %}